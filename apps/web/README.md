# Web (Next.js)

## Run Locally

```bash
pnpm install
pnpm dev
```

## Environment Variables

- `NEXT_PUBLIC_API_BASE_URL` (default: `http://localhost:8000`)

## Build

```bash
pnpm build
pnpm start
```
